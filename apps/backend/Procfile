# Railway/Heroku-style process definitions. On Railway, create TWO services from
# this repo (root: apps/backend) — one using the `web` command, one using `worker`.
web: alembic upgrade head && uvicorn app.main:asgi --host 0.0.0.0 --port $PORT
worker: python -m app.worker.main
